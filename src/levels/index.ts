import type { Level } from './format';
import wiese from './wiese';
import wueste from './wueste';

/** Alle Level in der Reihenfolge, in der sie freigeschaltet werden. */
export const LEVELS: Level[] = [wiese, wueste];
